---
index: 0
title: 下拉框不跟随页面滚动的问题
description: 在使用 antd 组件库的时候，下拉框（Select）是不会跟随页面进行滚动的，一般来说没什么问题。但是如果UED需要组件实现下拉框也能跟随怎么办呢？
author: 杨飞
tags: [前端, antd]
date: 2021-06-09 10:50:32
---

> 在使用 antd 组件库的时候，下拉框（Select）是不会跟随页面进行滚动的，一般来说没什么问题。
> 但是如果UED需要组件实现下拉框也能跟随怎么办呢？


方案一: 跟随指定的一个外部元素
```javascript
const Hello = () => (
  <div style={{ margin: 10, overflow: 'scroll', height: 200 }}>
    <h2>Select in a scrollable area</h2>
    <div style={{ padding: 100, height: 1000, background: '#eee', position: 'relative' }} id="area">
      <h4>可滚动的区域 / scrollable area</h4>
      <Select
        defaultValue="lucy"
        style={{ width: 120 }}
        getPopupContainer={() => document.getElementById('area')} // 指定需要跟随的上级对象
      >
        <Option value="jack">Jack</Option>
        <Option value="lucy">Lucy</Option>
        <Option value="yiminghe">yiminghe</Option>
      </Select>
    </div>
  </div>
);
```

方案二: 跟随上级元素
```javascript
const Hello = () => (
  <div style={{ margin: 10, overflow: "scroll", height: 200 }}>
    <h2>Select in a scrollable area</h2>
    <div
      style={{
        padding: 100,
        height: 1000,
        background: "#eee",
        position: "relative"
      }}
    >
      <h4>可滚动的区域 / scrollable area</h4>
      <Select
        defaultValue="lucy"
        style={{ width: 120 }}
        getPopupContainer={(triggerNode) => triggerNode.parentNode}
      >
        <Option value="jack">Jack</Option>
        <Option value="lucy">Lucy</Option>
        <Option value="yiminghe">yiminghe</Option>
      </Select>
    </div>
  </div>
);
```

遇到问题不要慌，多看官网文档，多用搜索引擎，你遇到的问题，大概率已经被其他人遇到过了:)